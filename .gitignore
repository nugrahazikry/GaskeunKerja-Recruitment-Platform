# Secrets
.env

# Brainstorming reference material (Tahap 2 code copy, design docs) — not part of the MVP build
brainstorming result/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / frontend
node_modules/
dist/
.vite/

# Docker / data volumes
postgres_data/
qdrant_data/

# Local storage (CVs, interview audio — never commit real candidate data)
storage/

# Seed source CVs — real third-party resumes (personal data), never commit to a public repo.
# The 2 test audio clips in seed/raw/audio/ (the user's own voice) are the one exception, already tracked.
seed/raw/cv/

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# graphify local knowledge graph (regeneratable via `graphify .`)
graphify-out/
